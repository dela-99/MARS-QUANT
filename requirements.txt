# M.A.R.S. core research stack
# Recommended Python: 3.10–3.13 (3.14 may lack wheels for some deps)
pandas>=2.0
numpy>=1.24
pytz
pyarrow
scikit-learn>=1.3
xgboost>=2.0
joblib
matplotlib
seaborn
python-dotenv
pytest

# Macro data
fredapi

# MetaTrader 5 (Windows; optional for download / live bots)
MetaTrader5

# ───────────────────────────────────────────────────────────────
# Research / optional — NOT required for live trading
# ───────────────────────────────────────────────────────────────
# Richer TA library (needs compatible Python / numba)
# pandas_ta only needed for add_advanced_indicators(); baseline uses pure pandas fallback
pandas_ta
# Alternative parquet engine (pyarrow is primary)
fastparquet

# Deep learning (baseline XGB does not require torch)
# torch only needed for LSTMClassifier/TransformerClassifier in research
torch

# Backtesting / charts (legacy + research)
backtesting
mplfinance